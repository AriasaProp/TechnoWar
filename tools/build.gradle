plugins {
    id 'cpp-application'
}

application {
		source.from file('main')
		privateHeaders.from file('main')
}

tasks.register("test") {
    dependsOn tasks.named("build")
    doLast {
        exec {
            commandLine "${project.buildDir}/tools"
            args("Hai", "Hello World")
        }
    }
}
